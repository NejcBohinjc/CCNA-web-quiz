/* CCNA1 Question Explanations
   Shown to the user after an incorrect answer.
   Key: question id (string) → explanation text */
window.EXPLANATIONS = {
  "1":  "RTP (Real-Time Transport Protocol) is specifically designed for streaming time-sensitive media. Voice and video are the two traffic types that use RTP because they require continuous, low-latency delivery. Web traffic uses HTTP/TCP, file transfers use FTP/TCP, and peer-to-peer is a network architecture, not a traffic type served by RTP.",
  "2":  "ZigBee (IEEE 802.15.4) is designed for low-power, low-data-rate wireless communication, making it ideal for home automation sensors and smart devices that must run for years on small batteries. LoRaWAN targets long-range outdoor IoT. 5G is high-bandwidth and power-hungry. Wi-Fi is also too power-intensive for most home automation sensors.",
  "3":  "The TCP/IP Internet layer (equivalent to OSI Layer 3) is responsible for logical addressing and routing packets across multiple networks using IP addresses. The Application layer handles user services. The Transport layer provides end-to-end communication. The Network Access layer deals with the physical medium.",
  "4":  "DNS (Domain Name System) uses resource records to resolve names to addresses and provide other information. Record types A (IPv4), AAAA (IPv6), NS (name server), and MX (mail exchanger) are all DNS-specific record types. Email, file, and web servers use application protocols, not DNS record types, to deliver their services.",
  "5":  "Proprietary protocols are owned and controlled by a specific organization, which defines and manages how they operate. Open standards protocols like TCP/IP are freely usable by anyone. Proprietary protocols may run on any hardware and are not necessarily the same as the TCP/IP suite.",
  "6":  "DNS (Domain Name System) translates human-readable domain names into IP addresses. HTTP defines rules for web content exchange. FTP handles file transfers between client and server. HTTPS adds encryption to web communication. DNS is the foundational service that makes browsing by name possible.",
  "7":  "Port 110 is the well-known port for POP3 (Post Office Protocol version 3), used by email clients to download messages from a mail server. DNS uses port 53. DHCP uses ports 67/68. SMTP uses port 25 for sending email.",
  "8":  "`ipconfig` is the Windows command that displays TCP/IP configuration including IP address, subnet mask, and default gateway. `show ip interface brief` and `show interfaces` are Cisco IOS commands used on routers and switches, not PCs. `ping` tests connectivity but does not display configuration.",
  "9":  "In a peer-to-peer (P2P) network, devices can act as both clients and servers without a dedicated central server. Sharing a printer directly from a home PC is a classic P2P scenario. A client-based model requires dedicated servers. Point-to-point describes a direct two-node link. Master-slave is a control relationship, not a network sharing model.",
  "10": "A virus is malicious software or code that runs on an end device, typically attaching itself to legitimate programs. A firewall is a network device that filters traffic. Stolen credentials describe identity theft. A DoS attack slows or crashes services. The defining trait of a virus is that it is software actively running on the end device.",
  "11": "QoS (Quality of Service) prioritizes real-time audio (voice conference) highest because any delay causes audible degradation. Financial transactions are next because data accuracy is critical. Web page traffic is lowest because small delays are imperceptible to users browsing static content.",
  "13": "The TCP/IP model has four layers and all four are used in any complete end-to-end file transfer: Application (FTP), Transport (TCP), Internet (IP), and Network Access (Ethernet). Options listing fewer layers are incomplete. The OSI 7-layer model is a different model — the question asks specifically about TCP/IP layers.",
  "15": "A host's default gateway must be an interface on the same local subnet as the host. R1's G0/0 (Gigabit Ethernet interface) connects to the same LAN as H1 and is therefore the correct default gateway address. Serial interfaces (S0/0/0, S0/0/1) connect routers to each other on WAN links and are on different subnets.",
  "16": "Internet Messenger (IM) applications provide real-time text, voice, and video communication between remote users. Remote device access uses SSH or Telnet. Domain name resolution is provided by DNS. Secure remote access is provided by SSH or VPN, not IM.",
  "18": "ARP (Address Resolution Protocol) builds and maintains the ARP cache, which maps IP addresses to MAC addresses — exactly what `arp -a` displays. DHCP dynamically assigns IP addresses. DNS resolves domain names. ICMP handles network error reporting and diagnostics.",
  "19": "RFI (Radio Frequency Interference) and EMI (Electromagnetic Interference) are both forms of external interference produced by fluorescent lights and electrical equipment that can corrupt signals on nearby copper cabling. Crosstalk is interference between wire pairs within the same cable, not from external sources. Signal attenuation is distance-related signal loss, not caused by nearby electrical equipment.",
  "20": "When a host needs to reach a device on a remote network, it must send the packet to its default gateway (router). Since the host doesn't know the router's MAC address, it broadcasts an ARP request for the gateway's MAC — not the remote device's MAC, which is irrelevant at the local Layer 2 level. The remote device's MAC address cannot be used across routed network boundaries.",
  "22": "Port 53 is the well-known port for DNS (Domain Name System), used for both UDP (queries) and TCP (zone transfers). POP3 uses port 110. IMAP uses port 143. NetBIOS uses ports 137–139.",
  "23": "A /27 subnet mask (255.255.255.224) provides 32 addresses with 30 usable hosts, which is sufficient for 25 devices. A /28 (255.255.255.240) gives only 14 usable hosts — too few. A /26 gives 62 hosts — more than needed, wasting addresses. The /27 is the smallest mask that can accommodate 25 hosts.",
  "24": "A Trojan horse is malicious software disguised as a legitimate application. Unlike viruses, Trojans do not self-replicate; they rely on the user to install them willingly. A DoS attack crashes services. Identity theft uses stolen credentials. A firewall is a defensive network device, not a threat type.",
  "25": "HTTPS (HTTP Secure) wraps HTTP in SSL/TLS encryption, securing the exchange of all web content including text, images, sound, and video. SSH provides encrypted remote device access. DNS resolves names to IP addresses. Telnet provides unencrypted remote access and is not a secure protocol.",
  "26": "The Transport layer uses source port numbers to distinguish multiple simultaneous application sessions on the same host. Each application gets a unique ephemeral source port so incoming data can be routed to the correct application process. MAC addresses identify devices at Layer 2. IP addresses identify hosts at Layer 3, not individual applications.",
  "27": "A /26 subnet mask (255.255.255.192) provides 64 addresses with 62 usable hosts, sufficient for 61 devices. A /25 gives 126 usable hosts — wasteful. A /27 gives only 30 usable hosts — not enough for 61. The /26 is the smallest mask that fits exactly 61 hosts.",
  "29": "A DoS (Denial of Service) attack floods or overwhelms a device or service, causing it to slow down or crash and become unavailable to legitimate users. Stolen credentials describe identity theft. A firewall is a defensive tool. Spyware secretly collects user information.",
  "31": "SMTP (Simple Mail Transfer Protocol) is an outbound protocol that sends email from mail clients to mail servers and between mail servers. POP3 and IMAP are inbound protocols for retrieving email. SSH provides encrypted remote access. IM provides real-time chat.",
  "32": "The Transport layer uses port numbers to multiplex multiple sessions. Two open browser windows each have different socket connections (different ports), allowing the transport layer to deliver each web page response to the correct window. MAC addresses are Layer 2 identifiers. Screen formatting is handled at the Application/Presentation layer.",
  "33": "When Host B sends to the router, the Layer 3 IP addresses are source=172.16.10.200 (Host B) and destination=172.16.20.200 (Host D). At Layer 2, the destination MAC is the router's interface (00-00-0c-94-36-ab) because the router is the next hop — not Host D's MAC, which is irrelevant until the packet reaches Host D's subnet.",
  "34": "Attenuation is the loss of signal strength as a signal travels over increasing distance through any transmission medium. Signal strengthening is called amplification. Signal leakage between cable pairs is crosstalk. The time for a signal to reach its destination is called propagation delay.",
  "35": "The `configure terminal` command requires the router to be in privileged EXEC mode (prompt shows `#`). User EXEC mode (prompt shows `>`) does not permit access to global configuration. The administrator must first type `enable` to reach privileged EXEC mode before issuing `configure terminal`.",
  "36": "The Application layer is the top layer of the TCP/IP model and contains protocols like DNS (domain name resolution) and POP (email retrieval). TCP and UDP operate at the Transport layer. IP operates at the Internet layer. Ethernet operates at the Network Access layer.",
  "37": "AAA stands for Authentication (verifying identity), Authorization (determining what actions are permitted), and Accounting (tracking what was done). Defining access rights — Read-Only for everyone vs Edit for admins — is an Authorization function. Automation is not part of the AAA framework.",
  "38": "Protocols defining network message transmission must specify: message size (maximum payload), message encoding (how data is formatted/structured), and delivery options (timing, sequencing, error handling). Connector specifications and media selection are physical/hardware concerns. End-device installation is an operational task outside of protocol scope.",
  "39": "IP is connectionless — it sends each packet independently without establishing a dedicated path first. IP is also media-independent — it operates over Ethernet, Wi-Fi, fiber, etc. IP does NOT guarantee delivery, reorder out-of-sequence packets, or retransmit lost packets — those are TCP functions at the Transport layer.",
  "40": "The scenario describes Security (remote VPN/encrypted access), Quality of Service (video conference maintained despite failover), and Fault Tolerance (automatic failover to a secondary ISP within seconds). Scalability refers to network growth capacity. Integrity refers to data accuracy. Powerline networking is a physical medium type.",
  "41": "UTP signal degradation is most commonly caused by improper termination (crimping errors or untwisting too much wire) and low-quality cables or connectors (introducing resistance and impedance mismatch). Shielding is relevant to STP cabling. Conduit installation is best practice and does not cause degradation. Light loss over distance applies to fiber optics, not copper UTP.",
  "42": "The subnet 192.168.1.64/26 covers addresses .64 through .127, so .96 is a valid usable host address. The /27 subnet at .32 covers .32–.63 (excludes .96). The /28 at .32 covers .32–.47 (too small). The /29 at .64 covers only .64–.71 (excludes .96).",
  "43": "A traceroute output showing replies at 4 hops to 192.168.100.1 confirms that connectivity exists and that 4 routers separate the source from the destination. The output does not indicate a missing default gateway (replies exist), videoconferencing capability, or specific average transmission times. Successful replies at each hop confirm the path is intact.",
  "44": "To accurately assess network traffic patterns, capture during peak utilization so all traffic types are visible, and capture on different segments to cover the whole network. Capturing only on weekends gives off-peak data. Focusing only on the data center misses client-side traffic. WAN traffic alone is not representative of all network activity.",
  "45": "The `ipv6 unicast-routing` command enables IPv6 routing on the device and causes all IPv6-enabled interfaces to begin sending ICMPv6 Router Advertisement (RA) messages. It does not automatically activate all interfaces, create global unicast addresses, or generate link-local addresses — those are created separately when IPv6 is configured on an interface.",
  "46": "The TCP/IP Application layer encompasses three OSI layers: Application (Layer 7), Presentation (Layer 6), and Session (Layer 5). The OSI Network layer maps to TCP/IP Internet. OSI Data Link and Physical map to TCP/IP Network Access. OSI Transport maps directly to TCP/IP Transport (one-to-one).",
  "47": "At each router hop, the Layer 2 frame header is stripped and a new one is created for the outgoing interface. R1 removes the Ethernet frame header (and trailer) from the original frame, reads the Layer 3 IP header to determine the next hop, then builds a new Layer 2 header appropriate for the S0/0/0 serial WAN interface before forwarding.",
  "48": "The default gateway is only consulted when communicating with hosts on different networks. Hosts on the same local subnet communicate directly without the gateway. If the gateway is misconfigured, local communication still works perfectly, but traffic destined for other networks cannot be routed. Pinging 127.0.0.1 (loopback) always works regardless of gateway settings.",
  "49": "ARP works by broadcasting a request to all local devices asking who has a specific IP address. The device with that IP replies with a unicast ARP reply containing its MAC address. ARP broadcasts only go to local hosts — not the target of the data packet. If no ARP reply is received, communication fails; the node does not broadcast the data.",
  "50": "A /25 subnet mask (255.255.255.128) provides 128 addresses with 126 usable hosts, enough for 90 devices. A /26 gives only 62 usable hosts — not enough for 90. A /27 gives 30. A /28 gives 14. The /25 is the smallest mask that can accommodate 90 hosts.",
  "51": "ICMPv6 added Neighbor Solicitation (NS) to replace IPv4 ARP for MAC address resolution, and Router Advertisement (RA) for stateless address autoconfiguration (SLAAC). Destination Unreachable, Time Exceeded, and Route Redirection all exist in both ICMPv4 and ICMPv6. Host Confirmation is not a defined ICMP message type.",
  "52": "Port 80 is the well-known port for HTTP (HyperText Transfer Protocol), used for unencrypted web communication. DHCP uses ports 67/68. SMTP uses port 25. DNS uses port 53. HTTPS uses port 443.",
  "53": "IMAP stores messages on the mail server permanently until manually deleted, allowing access from multiple devices and preserving a complete email history. POP3 downloads and typically deletes messages from the server, making them device-specific. IMAP does not send email — SMTP handles outbound mail for both protocols.",
  "54": "`nslookup` directly queries DNS servers to test name resolution. If a host can ping the IP address but not the URL, the issue is DNS — the domain name is not resolving correctly. `tracert` traces the route. `ipconfig` shows local IP configuration. `netstat` shows active connections.",
  "55": "The LLC (Logical Link Control) sublayer enables multiple Layer 3 protocols to share the same physical medium and identifies the encapsulated network layer protocol using the EtherType field (e.g., 0x0800 for IPv4, 0x86DD for IPv6). CSMA/CD and frame delimiting (preamble/SFD) are MAC sublayer functions.",
  "56": "The `ip default-gateway` command on a switch sets the router used for management traffic leaving the switch's local subnet. This enables remote management (Telnet/SSH) from administrators on other networks. It does not restrict all traffic, create an interface address, or affect normal data-plane frame forwarding.",
  "57": "The `transport input ssh` command restricts VTY line access to SSH only, which encrypts all data including the username, password, and session content. It does not enable the SSH client, enforce username/password authentication by itself, or require proprietary software — any standard SSH client works.",
  "59": "A reconnaissance attack involves passively gathering information about a target before launching an attack, such as using wireless scanning tools to map the enterprise network. DoS attacks disrupt service availability. Access attacks exploit vulnerabilities to gain unauthorized entry. Trojan horses are malware disguised as legitimate software.",
  "60": "HTTP (HyperText Transfer Protocol) defines the rules for requesting and delivering web content including text, images, audio, and video. HTTPS adds encryption. FTP transfers files. IM provides real-time chat. HTTP is the foundation of the World Wide Web.",
  "61": "Port 67 is the DHCP server port; clients broadcast to this port to request IP address configuration. The DHCP server responds using port 68. FTP uses ports 20/21. Telnet uses port 23. SSH uses port 22.",
  "62": "ARP requests are Layer 2 broadcasts, which means every device on the subnet must receive and process them. A large volume of ARP traffic floods the entire subnet, consuming CPU cycles on all hosts. ARP replies are unicast so they do not flood the network. Switches do not become overloaded from ARP alone since they handle MAC learning separately.",
  "63": "To diagnose inability to reach the Internet (but not local resources), you need: `ipconfig` to check IP/DNS configuration, `ping` to test connectivity to gateway and DNS server, and `nslookup` to verify DNS resolution. `arp -a` and netsh IPv6 commands target Layer 2/IPv6 issues. `telnet` tests TCP port connectivity, not DNS or routing.",
  "64": "When a router matches the destination IP to a directly connected interface in the routing table, no next-hop lookup is needed — the packet is immediately switched out that interface. For directly connected routes, the destination is on the same segment, so forwarding is immediate without further route table consultation.",
  "65": "Antispyware software runs on end devices and protects them from spyware and similar malware that collects user information. A firewall (hardware or software) filters network traffic. An ACL on a router filters traffic by IP address or application. A VPN provides encrypted tunneled access — it does not protect against malware on the device.",
  "66": "SSH (Secure Shell) encrypts the entire session — including the username, password, and all command output — making it the only option listed that keeps credentials and session content private. Telnet transmits everything in plaintext. Console requires physical access to the device. AUX port connections are also unencrypted.",
  "67": "The most effective malware defenses are keeping the OS and applications patched (eliminating known vulnerabilities) and using current antivirus software (detecting known malware signatures). VPNs protect network traffic but not the endpoint from malware. Firewalls block network-based attacks but not malware already on the device. Strong passwords and RAID do not prevent malware.",
  "68": "A Trojan horse disguises itself as a legitimate application (like a spreadsheet add-on) while performing hidden malicious actions such as disabling the firewall. Unlike viruses, Trojans do not self-replicate. DoS attacks crash services. Buffer overflows exploit memory boundaries. Brute-force attacks try many password combinations.",
  "69": "The Frame Check Sequence (FCS) field in an Ethernet frame contains a CRC value calculated by the source. The destination recalculates the CRC and compares it to the FCS — if they differ, the frame was corrupted and is discarded. The FCS operates at the Data Link layer, separate from transport-layer checksums used by TCP/UDP.",
  "70": "A /29 subnet mask (255.255.255.248) provides 8 addresses with 6 usable hosts, which is sufficient for 4 devices with room to spare. A /30 gives only 2 usable hosts — not enough. A /28 gives 14 usable hosts — sufficient but wasteful. The /29 is the smallest mask that comfortably fits 4 hosts.",
  "71": "POP3 (Post Office Protocol v3) retrieves email by downloading messages from the server to the local client, typically deleting them from the server afterward. IMAP keeps messages on the server. SSH provides encrypted remote device access. POP3 is specifically for email retrieval, not remote access to network devices.",
  "72": "VPNs (Virtual Private Networks) require enterprise-grade infrastructure and are complex to deploy. IPS (Intrusion Prevention Systems) require sophisticated hardware and tuning, making them cost-prohibitive for most home users. Antispyware, antivirus, and strong passwords are basic security measures available and appropriate for all users.",
  "73": "Antivirus software detects, blocks, and removes malicious software from end devices. It operates at the endpoint level. A hardware firewall is a network device. A software firewall on a router filters by IP/application. A VPN provides encrypted tunneled access. Antivirus is specifically end-device protection against malware.",
  "74": "Routers prevent routing loops using the TTL (Time to Live) field. Each router decrements TTL by 1. When TTL reaches 0, the router drops the packet and sends an ICMPv4 Time Exceeded message to the source. Routers do not check for a value of 100 or increment the TTL — only decrementation by 1 occurs at each hop.",
  "75": "Port 69 is the well-known port for TFTP (Trivial File Transfer Protocol), a simplified UDP-based protocol used for network booting and device firmware transfers. DNS uses 53. DHCP uses 67/68. SMTP uses 25. TFTP uses UDP rather than TCP for simplicity and speed.",
  "76": "To configure SSH on a Cisco router: you must configure the IP domain name (required to generate the RSA key pair), generate the SSH keys (RSA key pair for encryption), and enable inbound SSH on the VTY lines. DNS configuration is not required. Pre-shared keys are for IPSec VPN. Telnet sessions should be disabled when SSH is in use.",
  "77": "The MAC (Media Access Control) sublayer handles the FCS trailer for error detection and uses the preamble/SFD to delimit frame fields. The LLC sublayer handles identifying the encapsulated Layer 3 protocol (EtherType) and communicating with upper-layer software. Media access (CSMA/CD) is also a MAC sublayer function.",
  "78": "FF02::2 is the IPv6 all-routers link-local multicast address. All IPv6 routers join this multicast group, so packets to FF02::2 are received by all routers on the local link. FF02::1 is all-nodes (all IPv6 devices). Multicast addresses beginning with FF are not used for DHCP servers (FF02::1:2 is the DHCP multicast).",
  "79": "An IPv6 global unicast address (equivalent to a public IPv4 address) consists of: a global routing prefix (top bits assigned by the ISP, typically /48), a subnet ID (used by the organization to create subnets), and an interface ID (64 bits uniquely identifying the interface). Subnet mask is an IPv4 concept; IPv6 uses prefix notation. There is no broadcast in IPv6.",
  "80": "Wireless networks face three main design concerns: interference (from other wireless signals, microwaves, etc.), security (the wireless medium is accessible to anyone within range), and coverage area (walls and distance degrade signal). Extensive cabling is a wired networking concern. Collision avoidance (CSMA/CA) handles packet collisions. Mobility is an advantage, not a concern.",
  "81": "After setting a banner on a Cisco device, the fastest test is to exit privileged EXEC mode (`exit` or `logout`) and press Enter to initiate a new login session — the MOTD banner appears at that point. Power cycling and rebooting also work but are much slower. CTRL-Z returns you to privileged EXEC mode without logging out.",
  "82": "CSMA/CA (Carrier Sense Multiple Access with Collision Avoidance) is the access method used on wireless (IEEE 802.11) networks. Unlike wired Ethernet (CSMA/CD), wireless devices cannot detect collisions while transmitting, so they avoid them by waiting a random backoff time before transmitting. Token passing is used in legacy ring networks. CSMA/CD is used on wired Ethernet.",
  "83": "The Data Link layer is responsible for hop-to-hop (frame-by-frame) delivery of data between nodes on the same local media or point-to-point link. End-to-end delivery across networks is the Network layer's job. Formatting data is the Presentation layer's job. Application-to-application delivery involves the Transport and Application layers.",
  "84": "The TCP sliding window is a flow control mechanism where the receiver advertises how much data it can accept. By reducing the window size, the receiver tells the sender to slow its transmission rate. Reordering out-of-sequence segments and retransmission are separate TCP functions. The 3-way and 4-way handshakes handle connection setup and teardown.",
  "85": "Spyware is software that secretly installs on a user's device and collects information about the user (browsing habits, keystrokes, credentials) without consent, sending it to third parties. A firewall is a network device. DoS attacks crash services. Identity theft uses stolen credentials but is not software itself. Spyware specifically involves covert data collection on the device.",
  "86": "Store-and-forward switching receives and buffers the complete frame, checks the FCS for errors, and only forwards error-free frames. This eliminates corrupted frames from the network. Cut-through switching forwards immediately after reading the destination MAC, before the full frame is received, so it cannot check the FCS. Fragment-free reads the first 64 bytes only.",
  "87": "IPv6 link-local addresses must fall within the FE80::/10 range (binary prefix 1111 1110 10). These are automatically assigned to every IPv6-enabled interface and are only used for local-link communication — they are not routable. FEC0::/10 was the deprecated site-local range. FDEE::/7 is unique local (ULA). FF00::/8 is reserved for multicast.",
  "88": "FTP (File Transfer Protocol) provides client-server file transfer capability using ports 20 (data) and 21 (control). HTTP defines web content exchange rules. IM applications provide real-time chat. HTTPS secures web exchanges. FTP is specifically for transferring files between a client and a file server.",
  "89": "To access a website by domain name, the host needs a DNS server (to resolve the domain name to an IP address) and a default gateway (to route packets to the remote web server). The source port is automatically assigned by the OS. The HTTP server is on the remote host. The source MAC address is already present on the NIC.",
  "90": "NAT (Network Address Translation) conserves IPv4 address space by allowing many private addresses to share one public IP — this slows IPv4 address depletion. However, NAT breaks end-to-end connectivity for applications that embed IP addresses in their payload (some VoIP and FTP modes). NAT does not add authentication, affect switch performance, improve packet handling, or expand routing tables.",
  "91": "The EUI-64 process creates a 64-bit interface ID from a 48-bit MAC by: splitting the MAC at the midpoint, inserting FFFE between the two halves, and flipping the 7th bit (U/L bit). For 1C-6F-65-C2-BD-F8: insert FFFE → 1C6F:65FF:FEC2:BDF8. Flip bit 7 of 0x1C (0001 1100) → 0x1E (0001 1110) → the result is 1E6F:65FF:FEC2:BDF8.",
  "92": "ARP requests are broadcast within the subnet, so SW1 forwards the broadcast out all ports including the one connected to PC2. PC2 recognizes its own IP in the ARP request and sends a unicast ARP reply directly to PC1 with its MAC address. Switches transparently forward broadcasts but do not generate ARP replies. Routers only reply if proxy ARP is configured.",
  "93": "BOOTP (Bootstrap Protocol) is a legacy protocol that allowed diskless workstations to discover their IP address and boot server at startup. It has been superseded by DHCP. BOOTP uses static server-side mappings, whereas DHCP is dynamic. BOOTP does not transfer files or serve web content.",
  "94": "Adware is software that displays unwanted advertisements and may collect user behavior data, often bundled with free software. Like spyware, it gathers information but is primarily focused on advertising revenue. A firewall filters network traffic. Identity theft uses stolen credentials. DoS attacks crash services.",
  "95": "When a switch running in cut-through mode detects that the error rate on a port exceeds the configured threshold, it automatically reverts to store-and-forward mode, which performs full FCS error checking on every frame. This ensures corrupted frames are dropped rather than forwarded. The switch may return to cut-through if the error rate drops.",
  "97": "The Ethernet MAC sublayer has two primary responsibilities: accessing the media (using CSMA/CD for half-duplex Ethernet) and data encapsulation (framing — adding the MAC header with source/destination addresses and the FCS trailer). Error detection via FCS is part of encapsulation. Frame delimiting and logical addressing belong to the LLC sublayer.",
  "98": "The `show ip interface brief` output in the exhibit shows: Vlan1 with an IP address up/up (means an SVI is configured and the switch can be remotely managed), one physical interface showing up/up with one device attached, and the presence of the default SVI (Vlan1). Two physical interfaces up/up would mean two devices attached.",
  "99": "All four frame field types have distinct, defined roles: addressing directs the frame to its destination; error detection (FCS) verifies frame integrity; the type field (EtherType) identifies the Layer 3 protocol to the receiving LLC; and the frame start field (preamble/SFD) signals the start of the frame to receiving hardware. All matches are correct.",
  "100": "In a /64 IPv6 address, the first 64 bits represent the network/subnet portion. For 2001:DA48:FC5:A4:3D1B::1/64, the first four groups (2001:DA48:FC5:A4) form the subnet ID. The remaining bits (3D1B::1) are the interface ID. The subnet ID written with a /64 suffix is 2001:DA48:FC5:A4::/64.",
  "101": "Firewalls use multiple filtering mechanisms: application filtering blocks or allows traffic based on port numbers (application identity); packet filtering uses IP or MAC addresses; stateful packet inspection tracks connection state to block unsolicited inbound sessions; URL filtering controls access to specific websites. All four matches are correct.",
  "102": "A network performance baseline documents normal behavior metrics over time. When users report periodic slowdowns, comparing current measurements to the baseline shows whether this is expected peak-time behavior or an actual problem. Syslog records errors and events. Debug output and packet captures are for active troubleshooting. Config files show settings, not performance trends.",
  "103": "The `service password-encryption` command applies weak (Type 7) reversible encryption to plaintext passwords stored in the running and startup configuration files, preventing casual observers from reading them. It does not encrypt passwords transmitted over the network, enforce Telnet encryption, or require users to type encrypted passwords — it only protects the config file.",
  "104": "IPv6 retained the Source Address field name from IPv4. The IPv4 TTL field was renamed to Hop Limit in IPv6. The Version field still exists in IPv6 (value 6, not removed). The Destination Address field existed in IPv4 too (not new). The Header Checksum was removed from IPv6 — error checking is delegated to Layer 2 and upper layers.",
  "105": "The largest subnet (IP phones, 22 hosts) requires at least 30 usable addresses, so /27 (255.255.255.224, 30 usable) is needed. Since all four subnets must use the same mask, /27 is applied to all. A /28 (14 usable) is too small for 22 hosts. A /26 (62 usable) is unnecessarily large. The /27 is the only mask that works for the largest group while using the same mask throughout.",
  "106": "Identity theft involves stealing credentials (usernames, passwords, financial information) to impersonate a victim and gain unauthorized access to private accounts or data. An ACL/firewall filters network traffic. IPS software identifies spreading threats. VPN provides secure tunneled network access. Identity theft is specifically about using stolen credentials.",
  "107": "A /24 subnet mask (255.255.255.0) provides 256 addresses with 254 usable hosts, accommodating 200 devices. A /25 gives only 126 usable hosts — insufficient for 200. A /23 gives 510 usable hosts — wasteful. The /24 is the smallest single subnet mask that fits 200 hosts.",
  "108": "Industry cabling standards (TIA/EIA-568) define: cable lengths (maximum runs before signal degrades, e.g., 100m for UTP), pinouts (which wire connects to which pin — T568A and T568B), and connector types (RJ-45 for UTP, various fiber connectors). Cost per meter, connector color, and plastic tensile strength are not standardized installation requirements.",
  "109": "When terminating UTP cables, wires should be untwisted only as much as necessary to insert them into the connector — typically no more than 12mm (1/2 inch). Excessive untwisting removes the cross-talk protection that the twists provide, degrading signal quality. The connector type and removing the outer jacket are correct; wire gauge is not the issue shown.",
  "110": "All six matches are correct and reflect fundamental addressing characteristics: IP addresses are in Layer 3 (IP) headers; MAC addresses are in Layer 2 (Ethernet) frame headers; IP addresses are split into network and host portions; MAC addresses are split into OUI (vendor) and device-specific portions; IPv4 addresses are 32 bits (IPv6 are 128 bits); MAC addresses are always 48 bits.",
  "111": "Port 143 is the well-known port for IMAP (Internet Message Access Protocol), which allows clients to access and manage email stored on a mail server without downloading it permanently. FTP uses 20/21. SSH uses 22. Telnet uses 23. IMAP differs from POP3 (port 110) in that messages remain on the server.",
  "112": "Both TCP and UDP use port numbers to identify specific application services and include checksums to verify data integrity. TCP uses a 3-way handshake for connection setup; UDP is connectionless. Only TCP has a window size. Digitized voice typically uses UDP via RTP for low latency. Port numbering and checksum use are the two shared characteristics.",
  "113": "A /28 subnet provides 16 addresses (14 usable hosts), which is sufficient for 10 hosts while minimizing wasted addresses. Valid /28 subnets that fit within unused space in the 10.18.10.0/24 range (and don't overlap previously assigned subnets) include 10.18.10.208/28 (.208–.223) and 10.18.10.224/28 (.224–.239). A /27 gives 30 usable hosts — too many.",
  "114": "Port 21 is the well-known control port for FTP (File Transfer Protocol), used for sending commands. Port 20 is used for the actual data transfer in active mode. LDAP uses port 389. SLP uses port 427. SNMP uses ports 161 (queries) and 162 (traps).",
  "115": "A NIC's MAC (Media Access Control) address is a 48-bit hardware address burned into the NIC that operates at OSI Layer 2 (Data Link layer). MAC addresses are used for local-link frame delivery. IP addresses operate at Layer 3. The RJ-45 port and Ethernet cable operate at Layer 1 (Physical). The TCP/IP stack spans multiple layers."
};
